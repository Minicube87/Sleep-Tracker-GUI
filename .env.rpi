# Sleep Tracker - RPi Environment Configuration
# Kopiere diese Datei zu .env und setze deine echten Werte ein

# OpenAI API Key (von https://platform.openai.com/api-keys)
# ACHTUNG: Niemals ins Git-Repo committen!
OPENAI_API_KEY=sk-proj-your-actual-key-here

# Node Environment
NODE_ENV=production

# Server Port (für RPi, normalerweise 3000)
PORT=3000

# API Timeout in Milliseconds
API_TIMEOUT=30000

# Optional: Logging Level (debug, info, warn, error)
LOG_LEVEL=info
